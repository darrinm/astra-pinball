{
  "name": "candyland-pinball",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.js",
    "deploy": "npm run build && wrangler deploy",
    "test:clearance": "node tests/browser-clearance.mjs",
    "test:clearance:physics": "node tests/run-clearance-sweep.mjs"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.20.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "prettier": "^3.9.6",
    "vite": "^8.2.2",
    "wrangler": "^4.129.0"
  }
}
